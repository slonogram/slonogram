[tool.poetry]
name = "slonogram"
version = "0.1.0"
description = ""
authors = ["Nero <nerodono0@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[tool.poetry.group.dev.dependencies]
telegram-type-parser = { git = "https://github.com/nerodono/telegram_type_parser" }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
