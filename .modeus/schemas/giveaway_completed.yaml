data:
  GiveawayCompleted:
    fields:
      giveaway_message:
        description: Optional. Message with the giveaway that was completed, if it
          wasn't deleted
        name: giveaway_message
        required: false
        types:
        - Message
      unclaimed_prize_count:
        description: Optional. Number of undistributed prizes
        name: unclaimed_prize_count
        required: false
        types:
        - Integer
      winner_count:
        description: Number of winners in the giveaway
        name: winner_count
        required: true
        types:
        - Integer
    kind: struct
    meta:
      description:
      - This object represents a service message about the completion of a giveaway
        without public winners.
      href: https://core.telegram.org/bots/api#giveawaycompleted
      name: GiveawayCompleted
dirty: false
