data:
  description:
  - Describes data sent from a Web App to the bot.
  fields:
    data:
      description: The data. Be aware that a bad client can send arbitrary data in
        this field.
      required: true
      types:
      - String
    button_text:
      description: Text of the web_app keyboard button from which the Web App was
        opened. Be aware that a bad client can send arbitrary data in this field.
      required: true
      types:
      - String
  kind: struct
dirty: false
file_name: web_app_data.py
