data:
  description:
  - Represents a reaction added to a message along with the number of times it was
    added.
  fields:
    type:
      description: Type of the reaction
      required: true
      types:
      - ReactionType
    total_count:
      description: Number of times the reaction was added
      required: true
      types:
      - Integer
  kind: struct
dirty: false
file_name: reaction_count.py
