data:
- description:
  - This object represents a message.
  fields:
    message_id:
      description: Unique message identifier inside this chat
      required: true
      types:
      - Integer
    message_thread_id:
      description: Optional. Unique identifier of a message thread to which the message
        belongs; for supergroups only
      required: false
      types:
      - Integer
    from:
      description: Optional. Sender of the message; empty for messages sent to channels.
        For backward compatibility, the field contains a fake sender user in non-channel
        chats, if the message was sent on behalf of a chat.
      required: false
      types:
      - User
    sender_chat:
      description: Optional. Sender of the message, sent on behalf of a chat. For
        example, the channel itself for channel posts, the supergroup itself for messages
        from anonymous group administrators, the linked channel for messages automatically
        forwarded to the discussion group. For backward compatibility, the field from
        contains a fake sender user in non-channel chats, if the message was sent
        on behalf of a chat.
      required: false
      types:
      - Chat
    sender_boost_count:
      description: Optional. If the sender of the message boosted the chat, the number
        of boosts added by the user
      required: false
      types:
      - Integer
    date:
      description: Date the message was sent in Unix time. It is always a positive
        number, representing a valid date.
      required: true
      types:
      - Integer
    chat:
      description: Chat the message belongs to
      required: true
      types:
      - Chat
    forward_origin:
      description: Optional. Information about the original message for forwarded
        messages
      required: false
      types:
      - MessageOrigin
    is_topic_message:
      description: Optional. True, if the message is sent to a forum topic
      required: false
      types:
      - Boolean
    is_automatic_forward:
      description: Optional. True, if the message is a channel post that was automatically
        forwarded to the connected discussion group
      required: false
      types:
      - Boolean
    reply_to_message:
      description: Optional. For replies in the same chat and message thread, the
        original message. Note that the Message object in this field will not contain
        further reply_to_message fields even if it itself is a reply.
      required: false
      types:
      - Message
    external_reply:
      description: Optional. Information about the message that is being replied to,
        which may come from another chat or forum topic
      required: false
      types:
      - ExternalReplyInfo
    quote:
      description: Optional. For replies that quote part of the original message,
        the quoted part of the message
      required: false
      types:
      - TextQuote
    reply_to_story:
      description: Optional. For replies to a story, the original story
      required: false
      types:
      - Story
    via_bot:
      description: Optional. Bot through which the message was sent
      required: false
      types:
      - User
    edit_date:
      description: Optional. Date the message was last edited in Unix time
      required: false
      types:
      - Integer
    has_protected_content:
      description: Optional. True, if the message can't be forwarded
      required: false
      types:
      - Boolean
    media_group_id:
      description: Optional. The unique identifier of a media message group this message
        belongs to
      required: false
      types:
      - String
    author_signature:
      description: Optional. Signature of the post author for messages in channels,
        or the custom title of an anonymous group administrator
      required: false
      types:
      - String
    text:
      description: Optional. For text messages, the actual UTF-8 text of the message
      required: false
      types:
      - String
    entities:
      description: Optional. For text messages, special entities like usernames, URLs,
        bot commands, etc. that appear in the text
      required: false
      types:
      - Array of MessageEntity
    link_preview_options:
      description: Optional. Options used for link preview generation for the message,
        if it is a text message and link preview options were changed
      required: false
      types:
      - LinkPreviewOptions
    animation:
      description: Optional. Message is an animation, information about the animation.
        For backward compatibility, when this field is set, the document field will
        also be set
      required: false
      types:
      - Animation
    audio:
      description: Optional. Message is an audio file, information about the file
      required: false
      types:
      - Audio
    document:
      description: Optional. Message is a general file, information about the file
      required: false
      types:
      - Document
    photo:
      description: Optional. Message is a photo, available sizes of the photo
      required: false
      types:
      - Array of PhotoSize
    sticker:
      description: Optional. Message is a sticker, information about the sticker
      required: false
      types:
      - Sticker
    story:
      description: Optional. Message is a forwarded story
      required: false
      types:
      - Story
    video:
      description: Optional. Message is a video, information about the video
      required: false
      types:
      - Video
    video_note:
      description: Optional. Message is a video note, information about the video
        message
      required: false
      types:
      - VideoNote
    voice:
      description: Optional. Message is a voice message, information about the file
      required: false
      types:
      - Voice
    caption:
      description: Optional. Caption for the animation, audio, document, photo, video
        or voice
      required: false
      types:
      - String
    caption_entities:
      description: Optional. For messages with a caption, special entities like usernames,
        URLs, bot commands, etc. that appear in the caption
      required: false
      types:
      - Array of MessageEntity
    has_media_spoiler:
      description: Optional. True, if the message media is covered by a spoiler animation
      required: false
      types:
      - Boolean
    contact:
      description: Optional. Message is a shared contact, information about the contact
      required: false
      types:
      - Contact
    dice:
      description: Optional. Message is a dice with random value
      required: false
      types:
      - Dice
    game:
      description: 'Optional. Message is a game, information about the game. More
        about games: https://core.telegram.org/bots/api#games'
      required: false
      types:
      - Game
    poll:
      description: Optional. Message is a native poll, information about the poll
      required: false
      types:
      - Poll
    venue:
      description: Optional. Message is a venue, information about the venue. For
        backward compatibility, when this field is set, the location field will also
        be set
      required: false
      types:
      - Venue
    location:
      description: Optional. Message is a shared location, information about the location
      required: false
      types:
      - Location
    new_chat_members:
      description: Optional. New members that were added to the group or supergroup
        and information about them (the bot itself may be one of these members)
      required: false
      types:
      - Array of User
    left_chat_member:
      description: Optional. A member was removed from the group, information about
        them (this member may be the bot itself)
      required: false
      types:
      - User
    new_chat_title:
      description: Optional. A chat title was changed to this value
      required: false
      types:
      - String
    new_chat_photo:
      description: Optional. A chat photo was change to this value
      required: false
      types:
      - Array of PhotoSize
    delete_chat_photo:
      description: 'Optional. Service message: the chat photo was deleted'
      required: false
      types:
      - Boolean
    group_chat_created:
      description: 'Optional. Service message: the group has been created'
      required: false
      types:
      - Boolean
    supergroup_chat_created:
      description: 'Optional. Service message: the supergroup has been created. This
        field can''t be received in a message coming through updates, because bot
        can''t be a member of a supergroup when it is created. It can only be found
        in reply_to_message if someone replies to a very first message in a directly
        created supergroup.'
      required: false
      types:
      - Boolean
    channel_chat_created:
      description: 'Optional. Service message: the channel has been created. This
        field can''t be received in a message coming through updates, because bot
        can''t be a member of a channel when it is created. It can only be found in
        reply_to_message if someone replies to a very first message in a channel.'
      required: false
      types:
      - Boolean
    message_auto_delete_timer_changed:
      description: 'Optional. Service message: auto-delete timer settings changed
        in the chat'
      required: false
      types:
      - MessageAutoDeleteTimerChanged
    migrate_to_chat_id:
      description: Optional. The group has been migrated to a supergroup with the
        specified identifier. This number may have more than 32 significant bits and
        some programming languages may have difficulty/silent defects in interpreting
        it. But it has at most 52 significant bits, so a signed 64-bit integer or
        double-precision float type are safe for storing this identifier.
      required: false
      types:
      - Integer
    migrate_from_chat_id:
      description: Optional. The supergroup has been migrated from a group with the
        specified identifier. This number may have more than 32 significant bits and
        some programming languages may have difficulty/silent defects in interpreting
        it. But it has at most 52 significant bits, so a signed 64-bit integer or
        double-precision float type are safe for storing this identifier.
      required: false
      types:
      - Integer
    pinned_message:
      description: Optional. Specified message was pinned. Note that the Message object
        in this field will not contain further reply_to_message fields even if it
        itself is a reply.
      required: false
      types:
      - MaybeInaccessibleMessage
    invoice:
      description: 'Optional. Message is an invoice for a payment, information about
        the invoice. More about payments: https://core.telegram.org/bots/api#payments'
      required: false
      types:
      - Invoice
    successful_payment:
      description: 'Optional. Message is a service message about a successful payment,
        information about the payment. More about payments: https://core.telegram.org/bots/api#payments'
      required: false
      types:
      - SuccessfulPayment
    users_shared:
      description: 'Optional. Service message: users were shared with the bot'
      required: false
      types:
      - UsersShared
    chat_shared:
      description: 'Optional. Service message: a chat was shared with the bot'
      required: false
      types:
      - ChatShared
    connected_website:
      description: 'Optional. The domain name of the website on which the user has
        logged in. More about Telegram Login: https://core.telegram.org/widgets/login'
      required: false
      types:
      - String
    write_access_allowed:
      description: 'Optional. Service message: the user allowed the bot to write messages
        after adding it to the attachment or side menu, launching a Web App from a
        link, or accepting an explicit request from a Web App sent by the method requestWriteAccess'
      required: false
      types:
      - WriteAccessAllowed
    passport_data:
      description: Optional. Telegram Passport data
      required: false
      types:
      - PassportData
    proximity_alert_triggered:
      description: Optional. Service message. A user in the chat triggered another
        user's proximity alert while sharing Live Location.
      required: false
      types:
      - ProximityAlertTriggered
    boost_added:
      description: 'Optional. Service message: user boosted the chat'
      required: false
      types:
      - ChatBoostAdded
    forum_topic_created:
      description: 'Optional. Service message: forum topic created'
      required: false
      types:
      - ForumTopicCreated
    forum_topic_edited:
      description: 'Optional. Service message: forum topic edited'
      required: false
      types:
      - ForumTopicEdited
    forum_topic_closed:
      description: 'Optional. Service message: forum topic closed'
      required: false
      types:
      - ForumTopicClosed
    forum_topic_reopened:
      description: 'Optional. Service message: forum topic reopened'
      required: false
      types:
      - ForumTopicReopened
    general_forum_topic_hidden:
      description: 'Optional. Service message: the ''General'' forum topic hidden'
      required: false
      types:
      - GeneralForumTopicHidden
    general_forum_topic_unhidden:
      description: 'Optional. Service message: the ''General'' forum topic unhidden'
      required: false
      types:
      - GeneralForumTopicUnhidden
    giveaway_created:
      description: 'Optional. Service message: a scheduled giveaway was created'
      required: false
      types:
      - GiveawayCreated
    giveaway:
      description: Optional. The message is a scheduled giveaway message
      required: false
      types:
      - Giveaway
    giveaway_winners:
      description: Optional. A giveaway with public winners was completed
      required: false
      types:
      - GiveawayWinners
    giveaway_completed:
      description: 'Optional. Service message: a giveaway without public winners was
        completed'
      required: false
      types:
      - GiveawayCompleted
    video_chat_scheduled:
      description: 'Optional. Service message: video chat scheduled'
      required: false
      types:
      - VideoChatScheduled
    video_chat_started:
      description: 'Optional. Service message: video chat started'
      required: false
      types:
      - VideoChatStarted
    video_chat_ended:
      description: 'Optional. Service message: video chat ended'
      required: false
      types:
      - VideoChatEnded
    video_chat_participants_invited:
      description: 'Optional. Service message: new participants invited to a video
        chat'
      required: false
      types:
      - VideoChatParticipantsInvited
    web_app_data:
      description: 'Optional. Service message: data sent by a Web App'
      required: false
      types:
      - WebAppData
    reply_markup:
      description: Optional. Inline keyboard attached to the message. login_url buttons
        are represented as ordinary url buttons.
      required: false
      types:
      - InlineKeyboardMarkup
  kind: struct
- description:
  - This object describes a message that was deleted or is otherwise inaccessible
    to the bot.
  fields:
    chat:
      description: Chat the message belonged to
      required: true
      types:
      - Chat
    message_id:
      description: Unique message identifier inside the chat
      required: true
      types:
      - Integer
    date:
      description: Always 0. The field can be used to differentiate regular and inaccessible
        messages.
      required: true
      types:
      - Integer
  kind: struct
- description:
  - This object describes a message that can be inaccessible to the bot. It can be
    one of
  - '- Message'
  - '- InaccessibleMessage'
  kind: type-alias
  union:
  - Message
  - InaccessibleMessage
dirty: false
file_name: maybe_inaccessible_message.py
