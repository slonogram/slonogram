data:
  KeyboardButtonPollType:
    fields:
      type:
        description: Optional. If quiz is passed, the user will be allowed to create
          only polls in the quiz mode. If regular is passed, only regular polls will
          be allowed. Otherwise, the user will be allowed to create a poll of any
          type.
        name: type
        required: false
        types:
        - String
    kind: struct
    meta:
      description:
      - This object represents type of a poll, which is allowed to be created and
        sent when the corresponding button is pressed.
      href: https://core.telegram.org/bots/api#keyboardbuttonpolltype
      name: KeyboardButtonPollType
dirty: false
