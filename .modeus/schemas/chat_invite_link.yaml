data:
  description:
  - Represents an invite link for a chat.
  fields:
    invite_link:
      description: The invite link. If the link was created by another chat administrator,
        then the second part of the link will be replaced with "...".
      required: true
      types:
      - String
    creator:
      description: Creator of the link
      required: true
      types:
      - User
    creates_join_request:
      description: True, if users joining the chat via the link need to be approved
        by chat administrators
      required: true
      types:
      - Boolean
    is_primary:
      description: True, if the link is primary
      required: true
      types:
      - Boolean
    is_revoked:
      description: True, if the link is revoked
      required: true
      types:
      - Boolean
    name:
      description: Optional. Invite link name
      required: false
      types:
      - String
    expire_date:
      description: Optional. Point in time (Unix timestamp) when the link will expire
        or has been expired
      required: false
      types:
      - Integer
    member_limit:
      description: Optional. The maximum number of users that can be members of the
        chat simultaneously after joining the chat via this invite link; 1-99999
      required: false
      types:
      - Integer
    pending_join_request_count:
      description: Optional. Number of pending join requests created using this link
      required: false
      types:
      - Integer
  kind: struct
dirty: false
file_name: chat_invite_link.py
