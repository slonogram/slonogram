data:
  description:
  - This object represents a phone contact.
  fields:
    phone_number:
      description: Contact's phone number
      required: true
      types:
      - String
    first_name:
      description: Contact's first name
      required: true
      types:
      - String
    last_name:
      description: Optional. Contact's last name
      required: false
      types:
      - String
    user_id:
      description: Optional. Contact's user identifier in Telegram. This number may
        have more than 32 significant bits and some programming languages may have
        difficulty/silent defects in interpreting it. But it has at most 52 significant
        bits, so a 64-bit integer or double-precision float type are safe for storing
        this identifier.
      required: false
      types:
      - Integer
    vcard:
      description: Optional. Additional data about the contact in the form of a vCard
      required: false
      types:
      - String
  kind: struct
dirty: false
file_name: contact.py
